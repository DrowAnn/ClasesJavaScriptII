/* //Scope

let variable = 44;

function contador() {
  let inicio = 0;

  if (inicio == 0) {
    let adicion = 1;
    console.log(`${variable} ${inicio} ${adicion}`);
  }
  //console.log(`${variable} ${inicio} ${adicion}`); No accede a la variable "adicion" dentro del if
  console.log(`${variable} ${inicio}`);
}

contador();
//console.log(`${variable} ${inicio} ${adicion}`); No accede a las varibles "inicio" y "adicion" dentro de la funcion contador
console.log(`${variable}`);*/

//
